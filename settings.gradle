pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //adding Jitpack to the app
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Parrot"
include ':app'
